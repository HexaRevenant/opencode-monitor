schema: spec-driven

context: |
  Project: opencode-system-metrics-tui
  Stack: TypeScript 7, Node.js ESM, SolidJS, OpenTUI, Bun build tooling, systeminformation, Koffi
  Architecture: OpenCode TUI plugin with native and fallback metric readers, Solid/OpenTUI presentation, and safe installation helpers
  Style: strict TypeScript, NodeNext modules, existing npm scripts, English technical artifacts
  Workspace root: /home/hexa/datos/Documentos/GitHub/opencode-monitor
  Detection: 2026-09-07; authoritative workspace contains one root project
  README: aligned with package scripts, live clock/date behavior, and current package version

strict_tdd: true

projects:
  - path: "."
    stack: "TypeScript 7 / Node.js ESM / SolidJS / OpenTUI"
    test_command: "npm test"
    test_framework: "tsx --test (Node.js test runner)"
    test_layers:
      unit: true
      integration: false
      e2e: false
    coverage_command: ""
    linter_command: ""
    typecheck_command: "npm run typecheck"
    formatter_command: ""
    build_command: "npm run build"

testing:
  strict_tdd: true
  workspace_test_command: "npm test"
  runner: "tsx --test test/*.test.ts"
  unit: true
  integration: false
  e2e: false
  coverage: false
  linter: false
  type_checker: "tsc --noEmit"
  formatter: false
  validation: "46 tests passed; npm run typecheck and npm run build passed on 2026-09-07"

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Document architecture decisions with rationale
    - Include sequence diagrams for complex flows
  tasks:
    - Group by phase and use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    guidelines:
      - Follow existing strict TypeScript and npm script conventions
    tdd: true
    test_command: "npm test"
  verify:
    test_command: "npm test"
    build_command: "npm run build"
    typecheck_command: "npm run typecheck"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
